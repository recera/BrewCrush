-- Phase 10: Billing System Cron Jobs (Fixed)
-- Skip the problematic comment on cron schema
-- The cron jobs will be created without that line

-- Note: pg_cron extension should already be enabled
-- If not, it needs to be enabled by superuser